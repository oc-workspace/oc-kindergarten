# 角色精灵版本

- `v1/`：基于第一版双足人物概念制作的现有角色精灵，现归档为第一版。
- `v2/`：第二版轮式人物设计；`idle` 和 `static` 精灵已经定稿。

新增或修改角色精灵时，请将资源放入对应版本目录，并在引用路径中明确写出版本号。

## 已知的第一版遗留引用

第一版中的部分转身元数据和旧生成脚本仍引用已经不存在的
`ai-agent-child-genderless/walking/v1`、`walking/v3` 素材。这些引用在本次归档前即已失效，
本次只为其补上 `v1/` 版本前缀，没有虚构或补生成缺失素材。
