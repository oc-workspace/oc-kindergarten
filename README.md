# OC Kindergarten

OC Kindergarten is a new OpenClaw workspace project scaffolded from the current `rococo-outreach` project style.

## Development

```bash
yarn install
yarn build
```

## Deployment Shape

The project keeps the same Next.js standalone + Docker compose structure used by `rococo-outreach`.
